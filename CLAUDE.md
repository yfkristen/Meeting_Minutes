# Meeting Minutes 會議記錄產出流程

此 repo 用於將「會議錄音檔的逐字稿文字」轉換成正式的「會議記錄」。

## 目錄結構

```
transcripts/   會議錄音的逐字稿原文（使用者提供，原封不動保存）
minutes/       Claude 產出的會議記錄（草稿與定稿）
reference/     已定稿的範例配對（逐字稿 + 最終版會議記錄），作為格式與風格的依據
templates/     會議記錄格式範本
```

## 工作流程

### 階段一：格式校準（建立範本）

1. 使用者提供錄音逐字稿 → 存入 `transcripts/YYYY-MM-DD_會議名稱.md`
2. Claude 依 `templates/meeting_minutes_template.md` 摘要成會議記錄草稿 → 存入 `minutes/YYYY-MM-DD_會議名稱.md`
3. 使用者提供人工修改後的**最終版本** → 覆蓋 `minutes/` 中的草稿，並將「逐字稿 + 最終版」複製一份到 `reference/`
4. Claude 比對草稿與最終版的差異，把學到的格式、用語、詳略程度更新回 `templates/meeting_minutes_template.md` 與本檔案的「風格要點」章節

### 階段二：正式產出

1. 使用者貼上逐字稿（或提供檔案）
2. Claude：
   - 將逐字稿存入 `transcripts/`
   - **先閱讀 `reference/` 內所有範例配對與 `templates/meeting_minutes_template.md`**，掌握格式與風格
   - 產出會議記錄存入 `minutes/`，並在對話中完整呈現給使用者
3. 若使用者有修改，將修改後版本視為新的最終版，回到階段一第 3–4 步持續校準

## 固定會議資訊

- **雙週會**：每兩週召開一次（週四 16:00-17:00，可實體或線上），例如 2026-07-23、2026-08-06、2026-08-20、2026-09-03…
- 使用者貼逐字稿時若未註明日期，預設為最近一次雙週會的日期，並向使用者確認
- 檔名使用「雙週會」作為會議名稱，例如 `2026-08-06_雙週會.md`

## 檔名規則

- 一律使用 `YYYY-MM-DD_會議名稱` 格式，例如 `2026-08-06_產品週會.md`
- 逐字稿與對應的會議記錄使用相同檔名，分別放在 `transcripts/` 與 `minutes/`
- 若使用者未提供會議日期，向使用者確認；未提供會議名稱時，從逐字稿內容推斷並與使用者確認

## 產出原則

- 使用繁體中文（台灣用語）
- 忠於逐字稿內容，不添加逐字稿中沒有的資訊；聽不清楚或語意不明處以「（待確認）」標註
- 口語轉為書面語，去除贅詞、重複與離題閒聊
- 決議事項與待辦事項（負責人、期限）務必完整擷取，這是會議記錄最重要的部分
- 發言人姓名依逐字稿標示；若逐字稿未區分發言人，不要憑空指派

## 風格要點（隨最終版本回饋持續更新）

依據 `reference/` 內四份定稿（2026-07-23、2026-08-06、2026-08-20、2026-09-03）歸納：

1. **本文起首**：以會議全名單獨成行，即「金控AI推動委員會PMO待辦事項討論例會」。
2. **信件外框（視寄送需要加上）**：7/23 定稿為信件形式，前後各加固定段落；8/6、8/20 定稿僅有本文。產出時預設只產本文，使用者要寄送時再套用下列外框：
   ```
   各位主管好：

   檢送{YYYY/MM/DD(週別)}「{完整會議名稱}」會議紀錄，敬請參閱。
   ```
   結尾：
   ```
   若有任何問題，再請聯繫我，謝謝！

   Best,
   ```
3. **大項結構固定三項**：壹＝會議時間、貳＝與會人員、參＝討論議題與會議紀要。所有議題一律放在「參」底下。
4. **編號層級**：大項「壹、貳、參」→「一、二、三」→「1. 2.」→「(1) (2)」。
5. **「參」底下的分節**：第一節為本次主軸議題（依內容命名，如「8月主管AI訓練行前課程」「9月工作坊(9/10-11)執行協調」），接著固定一節「各案進度」。新題目有兩種寫法：獨立成一節「三、新增議題」（8/6），或直接列在「各案進度」中並於標題後標註「(新增)」（9/3），視題目多寡擇一。首次會議（7/23）另有「PMO雙週會目的」「會議安排」兩節，屬建置期內容，後續會議不再重複。
6. **會議時間格式**：`2026/8/20(四)PM4:00-5:00`，用 PM/AM、冒號、半形連字號；括號與 PM 之間可不留空格。
7. **與會人員**：全名頓號分隔；請假者括號註記，簡記 `劉彥甫(請假)`，必要時寫原因 `歐黛瑩(會議衝堂請假)`。
8. **負責人標註**：各案進度與行動項目結尾以半形括號標註，單人 `(邱錦瑩)`、多人 `(唐可芹、劉宜依)`（跨單位共同事項可列至四人），分工不同者用斜線 `(柯燕茹/黃啟龍)`。分節標題下若統一標註，則各子項不再重複；(1)(2) 子項可只在其中一項標註負責人。
9. **用語**：正式、公文化，多用並列與四字語；行動項目用請託語氣「請……」，並把日期、梯次、期限寫入句中（如「共分三梯次辦理 (8/10、8/17及8/20)」「目標今年底前完成」）。
10. **專有名詞保留英文原樣**：SPADE、API Key、Token、POC、agent、LLM、LLM Gateway、pre-production、M365、Copilot、SharePoint、webmail、Slack、Teams。
11. **常見用字校正**：資訊安全用「資安」（非「治安」）；委員會補全名「AI推動委員會」「科技運用管理委員會」；「資料分級」勿寫成「資料分析」；課程規劃冠月份「8月課程規劃」。
12. **逐字稿校正對照**（語音辨識同音錯字，須依上下文還原）：
    - SPAD/SAD/Say/SP/SARP → SPADE（惟 SHP/Sharepint/SARP 指 SharePoint 時另判）
    - SAK/SAKE/Slake/SL → Slack；TS/Tams → Teams；TR → Trae
    - 達哥/打哥 → DaGo；口拍/cow/CW → Copilot；WBIL → webmail
    - SB/死倍/十倍/賽克 → SPADE；LN位/LG位/LM給way/Lgway → LLM Gateway；N365/M36 → M365
    - 報復 → 報備；合歸/何規/德歸 → 合規；委外事項的「報步」→ 報備；國路/活路 → 出路
    - 消金/效金/消息（金融語境）→ 消費金融；各資/各知/各支/個資 → 個資；遮幣/板幣 → 遮蔽
    - 治安 → 資安；繳卷/焦卷 → 繳交；克制化 → 客製化；資料分析 → 資料分級（依上下文）
13. **取捨原則（篩選邏輯）**：四場的壓縮比約 3–5%（逐字稿 1.4–2.8 萬字 → 定稿 400–900 字）。判斷方式：

    **收錄**
    - 已定案的做法與安排，連同日期、梯次、時間、地點（如「共分三梯次辦理 (8/10、8/17及8/20)」「9/3至9/4寄發」「目標今年底前完成」）
    - 各交辦案的最新進度：一句現況 + 一句結論或後續走向
    - 需他人配合的請託事項（「請於歸還廠商前務必清除SPADE」「請增派專責助教」）
    - 風險、法遵與限制的**結論**（「合規初步結論為建議不採用，主因……」「屬對外服務之必要管控」）
    - 下一步與提報層級（提三長會議、提科技運用管理委員會、回報朱總）
    - 尚未定案者寫明狀態：「後續走向待議」「俟需求明確後提AI三長會議請示」「尚待證券資訊單位回覆」
    - 新題目：標註「(新增)」或另立「新增議題」一節

    **不收**
    - 閒聊、抱怨、玩笑，以及對長官或同仁的評論與人事臆測
    - 會議操作雜訊：收音、麥克風、誰到了沒、投影畫面、翻到第幾頁
    - 來回討論的過程；只寫最後落點，未收斂則以「待議」帶過
    - 長篇技術原理說明；壓縮成一句結論（例：LLM Gateway 上送必遮蔽屬必要管控）
    - 臨場執行細節與個人行程安排

    **每項寫法**：`{議題}：{現況}，{結論或後續}。({負責人})`，一般 1–3 句。

    **自我檢查**：兩週後有人翻這份紀錄，這句話會影響他要做什麼嗎？沒有人要做、也沒有定案的內容就刪掉。

> 收到後續會議的新定稿時，於 `reference/` 新增配對並補充或修訂本節。
